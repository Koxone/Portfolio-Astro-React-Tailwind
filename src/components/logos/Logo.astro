---
const { title, src, font, size = '50' } = Astro.props;
---

<div class="flex items-center gap-3">
  <img
    class={`max-w-[${size}px]  sm:max-w-auto`}
    src={`/${src}.png`}
    alt="blue feather icon"
  />
  <p class={`whitespace-nowrap font-[${font}] text-center text-4xl`}>
    {title}
  </p>
</div>
